#include "monty.h"
/**
 * doNothing - A function that does nothing (NOP - No operation).
 * @head: Stack head
 * @counter: Line number
 * Return: No return
 */
void doNothing(stack_t **head, unsigned int counter)
{
    (void)counter;
    (void)head;
}
