#include "monty.h"
/**
 * switchToStackMode - Switches the stack mode to stack mode.
 * @head: Stack head
 * @counter: Line number
 * Return: No return
 */
void switchToStackMode(stack_t **head, unsigned int counter)
{
    (void)head;
    (void)counter;
    bus.lifi = 0;
}
